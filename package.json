{
  "name": "class-tracker",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend",
    "start": "cd backend && node src/init-db.js && node src/index.js"
  }
}
