{
  "name": "class-tracker",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "npm install --prefix frontend --include=dev && npm run build --prefix frontend && npm install --prefix backend --omit=dev",
    "start": "cd backend && node src/init-db.js && node src/index.js",
    "tunnel": "node scripts/ngrok-tunnel.mjs",
    "tunnel:cf": "node scripts/cf-tunnel.mjs"
  },
  "devDependencies": {
    "@ngrok/ngrok": "^1.4.1"
  }
}
